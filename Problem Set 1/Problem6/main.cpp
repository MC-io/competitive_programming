#include <iostream>
#include <vector>

int main()
{
    int N {}, B{}, H{}, W{};
    std::cin >> N >> B >> H >> W;
    std::vector<int> hotels(H);

    for (int i = 0; i < H; i++)
    {
        std::cin >> H;
        std::vector<int> a(W);
        for (int j = 0; j < W; j++)
        {
            std::cin >> a[j];
        }
        


    }
    return 0;
}