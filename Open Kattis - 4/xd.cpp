#include <iostream>
#include <math.h>
#include <vector>

std::vector<std::pair<int,int>> inter_mall1(2002);
std::vector<std::pair<int,int>> inter_mall2(2002);


int main()
{
    for (int i = 0; i < 50000; i++)
    {
        std::cout << i << '\n';
    }

    return 0;
}